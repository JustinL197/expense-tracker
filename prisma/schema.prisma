generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model expenses {
  id        Int      @id @default(autoincrement())
  title     String
  category  String
  timestamp String
  amount    Decimal
}